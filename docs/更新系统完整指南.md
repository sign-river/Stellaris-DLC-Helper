# 更新系统完整指南

> **版本**: 2.0  
> **更新时间**: 2025年12月5日  
> **状态**: ✅ 生产就绪

---

## 📖 目录

1. [概述](#概述)
2. [最新修复 (v1.0.3)](#最新修复-v103)
3. [使用说明](#使用说明)
4. [技术架构](#技术架构)
5. [故障排除](#故障排除)

---

## 概述

### 🎯 设计目标

更新系统 v2.0 实现了：
- **快速更新**：从26秒优化至1-2秒（80%提升）
- **智能配置**：自动合并配置文件，保留用户设置
- **详细日志**：分步骤记录，便于诊断问题
- **可靠替换**：双重清理机制，无残留文件

### ✨ 快速对比

| 指标 | 旧系统 | 新系统 | 提升 |
|------|--------|--------|------|
| 更新速度 | 26秒 | 1-2秒 | 🚀 **92%** |
| 配置更新 | ❌ 跳过 | ✅ 智能合并 | **新增** |
| 日志记录 | ❌ 无 | ✅ 详细分步 | **新增** |
| 文件清理 | ❌ 残留 | ✅ 双重清理 | **100%** |

---

## 最新修复 (v1.0.3)

### 🚀 核心改进

#### 1. 更新速度优化

**问题**：
- 点击"立即重启"后等待15-26秒
- updater_helper 尝试替换自己，重试20次失败（浪费20秒）

**解决方案**：
```python
# updater_helper.py - 跳过自身替换
if os.path.basename(newp) == 'updater_helper.exe':
    logging.info(f"  ⊙ 跳过（保留 .new 文件供主程序启动时替换）")
    continue

# main.py - 启动时立即清理
if getattr(sys, 'frozen', False):
    _cleanup_leftover_new_files()
```

**结果**：
- ✅ 更新时间：26秒 → 1-2秒（92%提升）
- ✅ 无进程等待浪费
- ✅ updater_helper.exe 在下次启动时自动更新

#### 2. 配置文件智能合并

**问题**：
- 旧版本跳过 config.json 更新
- 版本号不更新（1.0.1 → 1.0.2 失败）
- 新增配置项无法添加

**解决方案**：
```python
def _merge_config(self, new_config_path: Path) -> None:
    """智能合并配置文件，保留用户设置"""
    # 保留用户自定义配置
    user_settings = {
        'game_path': current_config.get('game_path'),
        'steam_path': current_config.get('steam_path'),
        'server': {
            'sources': current_config.get('server', {}).get('sources', [])
        }
    }
    
    # 加载新配置
    with open(new_config_path, 'r', encoding='utf-8') as f:
        new_config = json.load(f)
    
    # 合并配置
    new_config['game_path'] = user_settings['game_path']
    new_config['steam_path'] = user_settings['steam_path']
    if user_settings['server']['sources']:
        new_config['server']['sources'] = user_settings['server']['sources']
```

**保留项**：
- ✅ `game_path` - 游戏安装路径
- ✅ `steam_path` - Steam 路径
- ✅ `server.sources` - 自定义服务器源

**更新项**：
- ✅ `version` - 版本号
- ✅ 新增的配置项
- ✅ 默认值的更新

#### 3. 详细日志系统

**日志位置**：
- `Stellaris_DLC_Cache/operation_logs/updater_helper.log`
- `Stellaris_DLC_Cache/operation_logs/updater.log`

**日志内容示例**：
```log
[步骤1/4] 等待主程序退出 (PID=12345)...
  ✔ 主程序已退出 (等待时间: 1.77秒)

[步骤2/4] 读取批处理配置...
  ✔ 配置加载成功 (2 个文件)

[步骤3/4] 开始替换 2 个文件...
  [1/2] 处理: Stellaris-DLC-Helper.exe
    ✔ 替换成功 (尝试 2 次, 耗时 1.01秒)
  [2/2] 处理: updater_helper.exe
    ⊙ 跳过（保留 .new 文件供主程序启动时替换）

[步骤4/4] 准备启动新版本程序...
  ✔ 程序已启动

总用时: 2.78秒
```

**状态符号**：
- ✔ 成功
- ✖ 失败
- ⚠ 警告
- ⊙ 跳过

#### 4. 双重清理机制

**问题**：updater_helper.exe.new 残留

**解决方案**：
```python
# 清理机制1: main.py 启动时立即清理（优先级最高）
def _cleanup_leftover_new_files():
    """程序启动时立即清理残留的 .new 文件"""
    if not getattr(sys, 'frozen', False):
        return
    
    app_root = Path(sys.executable).parent
    new_files = list(app_root.glob('*.new'))
    
    for new_file in new_files:
        target_file = new_file.with_suffix('')
        if target_file.name == 'updater_helper.exe':
            try:
                backup_path = target_file.with_suffix('.old')
                if target_file.exists():
                    shutil.copy2(target_file, backup_path)
                shutil.move(str(new_file), str(target_file))
                logger.info(f"✔ 已替换: {target_file.name}")
            except Exception as e:
                logger.warning(f"⚠ 替换失败: {e}")

# 清理机制2: AutoUpdater 初始化时清理（fallback）
class AutoUpdater:
    def __init__(self):
        self._cleanup_leftover_new_files()
```

**结果**：
- ✅ 启动时立即处理 updater_helper.exe.new
- ✅ 无残留文件
- ✅ 备份 .old 文件确保安全

---

## 使用说明

### 自动更新检测

程序启动时自动检测更新（首次启动延迟5秒，后续每2小时检测一次）：

```python
# 配置自动更新间隔
{
    "update": {
        "check_on_startup": true,
        "check_interval": 7200,  // 2小时
        "auto_download": false
    }
}
```

### 手动检查更新

**方法1**：菜单操作
1. 点击菜单栏 `帮助`
2. 选择 `检查更新`

**方法2**：快捷键
- `Ctrl+U` - 检查更新

### 更新流程

```
1. 检测新版本
   ↓
2. 显示更新对话框（可选下载或跳过）
   ↓
3. 下载更新包 (GitHub/Gitee 多源下载)
   ↓
4. 创建 .new 文件（不影响当前运行）
   ↓
5. 点击"立即重启"
   ↓
6. 启动 updater_helper.exe
   ↓
7. 主程序退出（1-2秒）
   ↓
8. updater_helper 替换文件（跳过自己）
   ↓
9. 启动新版本主程序
   ↓
10. 主程序启动时替换 updater_helper.exe
   ↓
11. 完成更新 ✅
```

### 更新文件管理

**位置**：设置 → 常规设置 → 更新文件管理

**功能**：
1. **清理临时文件**：清理程序目录中的 `.new` 文件
2. **清理备份文件**：清理 `backup/` 目录
3. **清理更新下载包**：清理系统临时目录的更新 zip 文件

**统计信息**：
- 实时显示各类文件的数量和大小
- 点击按钮前可查看将清理的文件数量

---

## 技术架构

### 核心组件

```
更新系统架构
├─ AutoUpdater (自动更新管理器)
│  ├─ 定时检测更新
│  ├─ 后台下载管理
│  └─ 启动时清理残留文件
│
├─ UpdateManager (更新管理器)
│  ├─ 版本检查
│  ├─ 下载协调
│  └─ 安装管理
│
├─ UpdateInstaller (安装器)
│  ├─ 文件解压
│  ├─ 创建 .new 文件
│  ├─ 配置文件合并
│  └─ 批处理配置生成
│
├─ updater_helper.exe (独立替换进程)
│  ├─ 等待主程序退出
│  ├─ 批量替换文件
│  ├─ 跳过自身替换
│  ├─ 启动新版本
│  └─ 详细日志记录
│
└─ UpdateDialog (UI界面)
   ├─ 进度显示
   ├─ 版本信息
   └─ 操作按钮
```

### 文件替换策略

#### 普通文件
```
1. 解压到 program.exe.new
2. updater_helper 替换：
   - 删除 program.exe.old
   - 重命名 program.exe → program.exe.old
   - 移动 program.exe.new → program.exe
3. 启动新版本
```

#### updater_helper.exe（特殊处理）
```
1. 解压到 updater_helper.exe.new
2. updater_helper 跳过替换（因为正在运行）
3. 启动新版本主程序
4. 主程序启动时检测 .new 文件
5. 主程序替换 updater_helper.exe:
   - 备份 updater_helper.exe → updater_helper.exe.old
   - 移动 updater_helper.exe.new → updater_helper.exe
```

#### config.json（智能合并）
```
1. 解压到 config.json.new
2. 读取当前 config.json（保留用户设置）
3. 读取 config.json.new（获取新配置）
4. 合并配置：
   - 保留：game_path, steam_path, server.sources
   - 更新：version, 新增字段, 默认值
5. 写入最终配置
6. 删除 config.json.new
```

### 错误处理

```python
# 文件替换重试机制
for attempt in range(10):  # 最多10次
    try:
        # 删除旧文件
        if os.path.exists(target_path):
            os.remove(target_path)
        
        # 移动新文件
        shutil.move(new_path, target_path)
        success = True
        break
    except PermissionError:
        if attempt < 9:
            time.sleep(1)  # 等待1秒后重试
        else:
            logger.error(f"替换失败（权限不足）: {target_path}")
    except Exception as e:
        logger.error(f"替换失败: {e}")
        break
```

---

## 故障排除

### 问题1: 更新后版本号未变化

**症状**：
- 更新完成，但 `关于` 对话框显示旧版本号

**可能原因**：
- config.json 未正确更新
- 读取了缓存的配置

**解决方案**：
1. 检查 `config.json` 中的 `version` 字段
2. 重启程序
3. 查看日志：`Stellaris_DLC_Cache/operation_logs/updater.log`

**预防措施**：
- v1.0.3+ 已修复（智能合并配置）

### 问题2: 残留 .new 文件

**症状**：
- 程序目录中有 `*.exe.new` 文件

**原因**：
- updater_helper 无法替换自己
- 文件被占用导致替换失败

**解决方案**：
```
自动解决（v1.0.3+）：
1. 重启程序
2. 程序启动时自动清理

手动解决：
1. 关闭程序
2. 删除 *.new 文件
3. 重启程序
```

### 问题3: 更新速度慢

**症状**：
- 点击"立即重启"后等待很久

**检查步骤**：
1. 查看 `updater_helper.log`
2. 确认是哪个步骤耗时长

**可能原因**：
- 主程序退出慢 → 检查是否有未保存数据
- 文件替换失败 → 检查文件权限
- updater_helper 版本旧 → 更新到 v1.0.3+

### 问题4: 下载失败

**症状**：
- 下载进度卡住或失败

**解决方案**：
1. 检查网络连接
2. 切换下载源（GitHub ↔ Gitee）
3. 手动下载更新包：
   ```
   GitHub: https://github.com/your-repo/releases
   Gitee: https://gitee.com/your-repo/releases
   ```

### 问题5: 无法检测更新

**症状**：
- 显示"已是最新版本"，但实际有新版本

**检查**：
1. 确认当前版本号：`帮助` → `关于`
2. 确认最新版本号：访问 GitHub Releases

**可能原因**：
- config.json 版本号错误
- 更新服务器 URL 配置错误

**解决方案**：
```json
// config.json
{
    "version": "1.0.3",  // 确保正确
    "update": {
        "check_url": "https://api.github.com/repos/.../releases/latest"
    }
}
```

### 日志文件位置

```
Stellaris_DLC_Cache/
├── operation_logs/
│   ├── updater.log          # 更新管理器日志
│   ├── updater_helper.log   # 文件替换日志
│   └── operations_*.json    # 操作历史记录
```

---

## 版本历史

### v1.0.3 (2025-12-05)
- ✅ 更新速度优化（26s → 1-2s）
- ✅ 配置文件智能合并
- ✅ 详细日志系统
- ✅ 双重清理机制
- ✅ updater_helper 自更新支持

### v1.0.2 (2025-12-04)
- ✅ 缓存文件完整性验证
- ✅ 源选择优化 bug 修复
- ✅ 性能监控优化

### v1.0.1 (2025-12-03)
- 初始版本

---

**文档维护者**: GitHub Copilot  
**最后更新**: 2025-12-05  
**文档状态**: ✅ 当前版本
